{{!-- Shared header portrait element (consolidation bead 9v7c). Full-size
     headers wrap this in their own .header-portrait cell; compact headers
     render it bare. Params: document. --}}
<img class="profile-img" src="{{document.img}}" data-action="editImage" data-field="img"
    alt="{{document.name}}" title="{{document.name}}" />